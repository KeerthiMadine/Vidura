# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE are ignored when building from the command line, check
# build.gradle files instead.

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/guide/performance_tuning#parallel_builds
# org.gradle.parallel=true

# AndroidX package structure to make it easier to discover and migrate existing, old-style libraries
# to AndroidX. An alias will be created for the old build library name, so you can update its version
# without changing anything else in the source code.
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enable the configuration cache feature
org.gradle.configuration-cache=true
